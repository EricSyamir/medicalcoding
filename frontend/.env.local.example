# Copy to .env.local for local development

# URL of the FastAPI backend (local or deployed)
BACKEND_URL=http://localhost:8000

# Set to "true" to serve mock data when no backend is available (Vercel demo mode)
DEMO_MODE=false
